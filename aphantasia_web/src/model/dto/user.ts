export interface User {
    id: string;
    username: string;
    color: string;
    bio: string;
    dateJoined: string;
}

export interface UserColorName {
    id: string;
    username: string;
    color: string;
}